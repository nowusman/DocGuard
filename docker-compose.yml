version: "3.9"

services:
  frontend:
    build:
      context: ./app
    container_name: docguard-frontend
    ports:
      - "8501:8501"
    restart: unless-stopped
    # Optional: Resource limits (recommended for typical workloads)
    # deploy:
    #   resources:
    #     limits:
    #       cpus: "4.0"
    #       memory: 8G

networks:
  default:
    name: docguard-net
